# SalesCode QueryGPT

Natural language to SQL — ask questions in plain English and get optimized SQL queries.

## Tech stack

- Vite, TypeScript, React
- shadcn-ui, Tailwind CSS
- Firebase (auth), Vercel backend (Gemini)

## Run locally

```sh
npm i
npm run dev
```

Create a `.env` file with your Firebase config (see `src/lib/firebase.ts` for required vars).

## Build

```sh
npm run build
```
